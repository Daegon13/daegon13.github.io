---
import Nav from '../components/Nav.astro';
import Footer from '../components/Footer.astro';

/* CSS global de Tailwind para empaquetado */
import '../styles/tailwind.css';

/* Props SEO base: si no pasás url, evitamos canonical vacío */
const { title, description, image = '/og-default.png', url = '' } = Astro.props;
---
<html lang="es" class="scroll-smooth">
  <head>
    <!-- Metadatos básicos -->
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />

    <!-- Open Graph / Twitter -->
    <meta property="og:type" content="website" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={image} />
    <meta property="og:url" content={url} />

        <!-- JSON-LD: Organization + WebSite -->
    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@graph": [
          {
            "@type": "Organization",
            "name": "Marin.dev",
            "url": "https://daegon13.github.io/",
            "logo": "https://daegon13.github.io/og-default.png",
            "sameAs": [
              "https://github.com/Daegon13"
            ]
          },
          {
            "@type": "WebSite",
            "url": "https://daegon13.github.io/",
            "name": "Portafolio de Desarrollo Web",
            "potentialAction": {
              "@type": "SearchAction",
              "target": "https://daegon13.github.io/buscar?q={search_term_string}",
              "query-input": "required name=search_term_string"
            }
          }
        ]
      }
    </script>
        <!-- JSON-LD: Service + Offer (resumen de tus paquetes) -->
    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "Service",
        "name": "Desarrollo web frontend",
        "provider": {
          "@type": "Person",
          "name": "Diego Marin",
          "url": "https://daegon13.github.io/"
        },
        "areaServed": "Uruguay y remoto",
        "hasOfferCatalog": {
          "@type": "OfferCatalog",
          "name": "Paquetes",
          "itemListElement": [
            {
              "@type": "Offer",
              "name": "Landing Express",
              "priceSpecification": { "@type": "PriceSpecification", "priceCurrency": "USD", "minPrice": 190, "maxPrice": 290 }
            },
            {
              "@type": "Offer",
              "name": "Web Corporativa",
              "priceSpecification": { "@type": "PriceSpecification", "priceCurrency": "USD", "minPrice": 390, "maxPrice": 690 }
            },
            {
              "@type": "Offer",
              "name": "Panel + Integraciones",
              "priceSpecification": { "@type": "PriceSpecification", "priceCurrency": "USD", "minPrice": 690, "maxPrice": 1190 }
            }
          ]
        }
      }
    </script>


    <!-- Canonical (solo si llega url por props) -->
    {url && <link rel="canonical" href={url} />}

    <link rel="icon" href="/favicon.svg" />
  </head>
  <body class="min-h-screen bg-white text-gray-900 antialiased">
    <a href="#contenido" class="sr-only focus:not-sr-only">Saltar al contenido</a>
    <Nav />
    <main id="contenido" class="mx-auto max-w-6xl px-4 sm:px-6 lg:px-8">
      <slot />
    </main>
    <Footer />
  </body>
</html>
